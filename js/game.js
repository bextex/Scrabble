import Players from './player.js';

export default class Game {

  async start() {

    this.players = [];
    this.players.length =
      this.players = [
        new Player(this, name),
        new Player(this, name)
      ]
  }

}